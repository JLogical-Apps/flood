name: environment_banner
description: App Component to display a banner which shows the current environment.

publish_to: "none"

environment:
  sdk: ">=3.2.0 <4.0.0"

version: 0.0.1

dependencies:
  flutter:
    sdk: flutter

  pond:
    git:
      url: git@github.com:jaboyc/jlogical_utils.git
      ref: "v3.2"
      path: packages/pond/pond
  environment:
    git:
      url: git@github.com:jaboyc/jlogical_utils.git
      ref: "v3.2"
      path: packages/pond/modules/environment

dev_dependencies:
  lints: ^2.0.0
  flutter_test:
    sdk: flutter
