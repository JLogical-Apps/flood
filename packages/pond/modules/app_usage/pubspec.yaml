name: app_usage
description: Module for storing whether the user has opened the app before.

publish_to: "none"

environment:
  sdk: ">=3.2.0 <4.0.0"

version: 0.0.1

dependencies:
  flutter:
    sdk: flutter

  collection: ^1.16.0
  version: ^3.0.0

  environment:
    path: ../../../pond/modules/environment
  log:
    path: ../../../pond/modules/log
  persistence:
    path: ../../../persistence/persistence
  pond:
    path: ../../../pond/pond
dev_dependencies:
  lints: ^2.0.0
  test: 1.24.6
