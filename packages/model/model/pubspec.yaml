name: model
description: Model utils for Flutter.

publish_to: "none"
resolution: workspace

environment:
  sdk: ^3.6.0

version: 0.0.1

dependencies:
  flutter:
    sdk: flutter

  collection: ^1.16.0
  flutter_hooks: ^0.21.1-pre.0
  provider: ^6.0.5

  model_core:
    path: ../../model/model_core
  log:
    path: ../../pond/modules/log
  style:
    path: ../../style
  utils:
    path: ../../utils/utils
dev_dependencies:
  lints: ^2.0.0
  test: ^1.24.6