name: model_core
description: Core for model utils

publish_to: "none"
resolution: workspace

environment:
  sdk: ^3.6.0

version: 0.0.1

dependencies:
  collection: ^1.16.0
  rxdart: ^0.28.0

  utils_core:
    path: ../../utils/utils_core
dev_dependencies:
  lints: ^2.0.0
  test: ^1.24.6