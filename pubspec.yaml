name: jlogical_utils
description: Utils for JLogical. 
version: 0.0.1

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  # Presentation
  flutter_colorpicker: 0.3.4
  flutter_picker: 1.1.5

  # Application

  # Backend
  intl: 0.16.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: