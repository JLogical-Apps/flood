name: jlogical_utils
description: Utils for JLogical. 
version: 0.0.1

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  # Presentation
  flutter_colorpicker: 0.3.4
  flutter_picker: 1.1.5

  # Application
  flutter_bloc: 6.1.1

  # Backend
  freezed_annotation: 0.12.0
  intl: 0.16.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: 1.10.3 # flutter packages pub run build_runner watch
  freezed: 0.12.2

flutter: