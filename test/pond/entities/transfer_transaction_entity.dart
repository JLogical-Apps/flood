import 'budget_transaction_entity.dart';
import 'transfer_transaction.dart';

class TransferTransactionEntity extends BudgetTransactionEntity<TransferTransaction> {
  TransferTransactionEntity(TransferTransaction initialValue) : super(initialValue: initialValue);

}